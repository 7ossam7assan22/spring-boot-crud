spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:6033/app_db
    username: db_user
    password: db_user_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
